// import { useAuth } from '../../../contexts/AuthContext';
import {
    NotesContainer
} from './Notes.styles';

const Notes = () => {
    // const { user } = useAuth();

    return (
        <NotesContainer>
        </NotesContainer>
    );
};

export default Notes;